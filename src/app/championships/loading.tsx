import { RouteLoading } from "@/components/RouteLoading";

export default function Loading() {
  return (
    <RouteLoading
      title="Calcul du championnat"
      message="On recalcule les classements, les points et les courses retenues."
    />
  );
}
